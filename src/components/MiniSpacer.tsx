import React from 'react'

const MiniSpacer = ({ children }) => {
    return (
        <div style={{ marginTop: `6vh`, marginBottom: `6vh` }}>
            {children}
        </div>
    )
}
export default MiniSpacer;
